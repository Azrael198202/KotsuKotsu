name: kotsukotsu_account_engine
description: Shared account, auth and billing engine for KotsuKotsu apps.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.7

dependencies:
  meta: ^1.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8
